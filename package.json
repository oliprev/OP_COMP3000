{
  "name": "op_comp3000",
  "version": "1.0.0",
  "description": "## Supervisor: Professor Nathan Clarke",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "start": "npm run start --prefix backend",
    "build": "npm run build --prefix frontend",
    "install_all": "concurrently \"npm install\" \"npm install --prefix frontend\" \"npm install --prefix backend\"",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.1.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
